{
  "allowedApiEndpoints": [
    { "method": "GET", "path": ".*", "comment": "Allow all GET requests (read-only operations like listing models)" },
    { "method": "POST", "path": "/chat/completions", "comment": "Allow POST requests for chat completions" }
  ]
}